Kytos Integration Tests
=======================

To be used by the core team to test the integration of all Kytos projects.
Requires docker. Install the requirements.txt and run:

```sh
python3.6 -m unittest -v tests
```
